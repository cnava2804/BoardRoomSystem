@using Microsoft.AspNetCore.Identity
@using BoardRoomSystem.Areas.Identity.Data


@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

<ul class="navbar-nav">
@if (SignInManager.IsSignedIn(User))
{
        <div class="nav navbar-right" style="margin-left:0px;">

            <ul class="nav navbar-nav navbar-left">
                <li>
                    <a id="manage" class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hola: @UserManager.GetUserName(User) </a>
                </li>
            </ul>

            <ul class="nav navbar-nav navbar-left">
                <li>
                    <form id="logoutForm" class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                        <button id="logout" type="submit" class="nav-link btn btn-danger" style="margin-top:8px; margin-right: 5px; margin-left: 5px;">Logout</button>
                    </form>
                </li>
            </ul>
            
        </div>
    
}
else
{
   <div class="nav navbar-right" style="margin-left:0px;">
       @*<ul class="nav navbar-nav navbar-right">
          <li><a class="nav-link text-dark" id="register" asp-area="Identity" asp-page="/Account/Register">Register</a></li>
       </ul>*@
       <ul class="nav navbar-nav navbar-right">
         <li> <a class="nav-link text-dark" style="color:black;" id="login" asp-area="Identity" asp-page="/Account/Login">Login</a></li>
       </ul>
   </div>
    
   @* <li class="nav-item">
        <a class="btn btn-primary text-dark" id="register" asp-area="Identity" asp-page="/Account/Register">Register</a>
    </li>
    <li class="nav-item">
        <a class="nav-link text-dark" id="login" asp-area="Identity" asp-page="/Account/Login">Login</a>
    </li>*@
}
</ul>

